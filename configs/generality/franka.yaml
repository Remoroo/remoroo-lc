# franka: generated by scripts/cell_from_urdf.py from
#   /Users/adhamghazali-remoroo/Library/CloudStorage/Dropbox/Remoroo/dev/remoroo_engine/curobo/curobo/content/assets/robot/franka_description/franka_panda.urdf
#
# Every field below is DERIVED from that URDF -- root link, actuated joints,
# tool frames (deepest leaves), rest posture (mid-range).  Nothing about this
# robot is hand-written here, which is the point: it is a generality check,
# not a tuned configuration.

lc_spec_version: "1.0"
name: franka

limits: ../limits.yaml
gains: ../gains.default.yaml

models:
  - name: franka
    urdf: /Users/adhamghazali-remoroo/Library/CloudStorage/Dropbox/Remoroo/dev/remoroo_engine/curobo/curobo/content/assets/robot/franka_description/franka_panda.urdf
    base:
      xyz: [0.0, 0.0, 0.0]
      rpy: [0.0, 0.0, 0.0]
    joints:
      [panda_joint1, panda_joint2, panda_joint3, panda_joint4, panda_joint5, panda_joint6, panda_joint7, panda_finger_joint1, panda_finger_joint2]
    rest: [0.0, 0.0, 0.0, -1.5708, 0.0, 1.8675, 0.0, 0.02, 0.02]

tcps:
  - name: ee_link
    model: franka
    frame: ee_link
    damping: { w_thresh: 0.001 }
  - name: panda_leftfinger
    model: franka
    frame: panda_leftfinger
    damping: { w_thresh: 0.001 }

environment: []

collision:
  self_pairs: true
  cross_model: true
  environment: true
